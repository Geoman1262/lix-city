/* LIX CITY v0.68 — Building Activity System loader
   Activity logic is attached by building-interiors.js. This file exposes the version marker
   and safely keeps the system available if loaded before the interior module. */
window.LIX_CITY_BUILDING_ACTIVITIES_VERSION = "0.68";
