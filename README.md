# LIX CITY v0.8

Living City update.

- Uses the exact approved `lix.png` and `coin.png` assets.
- City is now a larger scrollable world instead of a fixed board.
- Buildings are placed inside the world with depth/shadow styling.
- Lix moves through the larger city and the view follows Lix.
- Pet follows Lix when enabled.
- City Coins use the exact `coin.png` asset and can be collected for +5 Coins.
- Existing games/progression/local save are retained.

Upload/replace all 7 files from this package on GitHub:
`index.html`, `style.css`, `app.js`, `README.md`, `lix.png`, `coin.png`, `cellix-logo.webp`.

This remains a localStorage prototype; cloud save/server security comes later.
